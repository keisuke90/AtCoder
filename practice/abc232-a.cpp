#include <bits/stdc++.h>
#define rep(i, start, end) for(int i = start; i < end; i++)
using namespace std;

int main(){
    string s;
    cin >> s;

    cout << int(s[0]-'0') * int(s[2]-'0') << endl;

    return 0;
} 