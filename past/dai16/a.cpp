#include <bits/stdc++.h>
#define rep(i, start, end) for(int i = start; i < end; i++)
using namespace std;

int main(){
    int m;
    cin >> m;
    if(m >= 4 && m <= 9) cout << "Yes" << endl;
    else cout << "No" << endl;

    return 0;
} 