#include <bits/stdc++.h>
#define rep(i, start, end) for(int i = start; i < end; i++)
using namespace std;

int main(){
    string s;
    cin >> s;
    if(s[s.size()-1] == 'r') cout << "er" << endl;
    else cout << "ist" << endl;

    return 0;
} 