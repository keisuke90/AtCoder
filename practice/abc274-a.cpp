#include <bits/stdc++.h>
#define rep(i, start, end) for(int i = start; i < end; i++)
using namespace std;

int main(){
    double a, b;
    cin >> a >> b;
    cout << fixed << setprecision(3)  << b/a << endl;
    return 0;
} 