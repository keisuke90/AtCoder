#include <bits/stdc++.h>
#define rep(i, start, end) for(int i = start; i < end; i++)
using namespace std;

int main(){
    string s;
    cin >> s;
    cout << 0;
    rep(i, 0, 3) {
        cout << s[i];
    }
    cout << endl;

    return 0;
} 